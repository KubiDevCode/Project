{
    "buildCommand": "npm run build:prod",
    "outputDirectory": "build",
    "rewrites": [
        {
            "source": "/((?!api/.*).*)",
            "destination": "/index.html"
        }
    ]
}
